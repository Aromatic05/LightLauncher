import SwiftUI

// MARK: - 设置视图入口
struct SettingsView: View {
    var body: some View {
        MainSettingsView()
    }
}

// MARK: - 预览
struct SettingsView_Previews: PreviewProvider {
    static var previews: some View {
        SettingsView()
    }
}
